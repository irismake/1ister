import 'package:flutter/material.dart';

class UserMyList extends StatelessWidget {
  const UserMyList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 60, width: 80, color: Colors.amber);
  }
}
