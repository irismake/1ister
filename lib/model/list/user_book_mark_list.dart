import 'package:flutter/material.dart';

class UserBookMarkList extends StatelessWidget {
  const UserBookMarkList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 60, width: 80, color: Colors.red);
  }
}
